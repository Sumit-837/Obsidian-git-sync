// A perfect number is a positive integer that is equal to the sum of its proper positive divisors (excluding itself).

#include <stdio.h>

void rec_perfect_num(int n) {
	if(n < 1) {
		return;
	}

	sum = 
}

int main() {
	int n;
	printf("Enter your number: ");
	scanf("%d", &n);
	
	rec_perfect_num(n);
}
